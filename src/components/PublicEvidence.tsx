import { useEffect,useState } from "react";
import { ExternalLink, RefreshCw, Search, ShieldCheck } from "lucide-react";
import { supabase } from "../lib/supabase";
type Evidence={id:string;source_name:string;source_type:string;title:string;published_at:string|null;symbol:string|null;summary:string|null;source_url:string;authority_score:number|null;relevance_score:number|null;matched_terms:string[]|null;evidence_type:string|null;validation_status:string|null};
export default function PublicEvidence(){const[rows,setRows]=useState<Evidence[]>([]),[query,setQuery]=useState(""),[source,setSource]=useState("all"),[loading,setLoading]=useState(false);async function load(){setLoading(true);let q=supabase.from("internet_evidence").select("id,source_name,source_type,title,published_at,symbol,summary,source_url,authority_score,relevance_score,matched_terms,evidence_type,validation_status").eq("is_relevant",true).not("published_at","is",null).order("relevance_score",{ascending:false}).order("published_at",{ascending:false}).limit(300);if(query.trim())q=q.or(`title.ilike.%${query.trim()}%,summary.ilike.%${query.trim()}%,symbol.ilike.%${query.trim()}%`);if(source!=="all")q=q.eq("source_type",source);const{data}=await q;setRows((data||[]) as Evidence[]);setLoading(false)}useEffect(()=>{load()},[]);return <section className="system-page"><div className="system-heading"><div><div className="eyebrow">VERIFIED REAL-WORLD EVIDENCE</div><h1>Public Evidence</h1><p>Dated, event-relevant regulator, exchange, company, and news records used by the cross-reference engine.</p></div><button className="secondary-action" onClick={load}><RefreshCw size={16}/>{loading?"Refreshing…":"Refresh"}</button></div><div className="evidence-toolbar"><div className="search-field"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} placeholder="Search company, ticker, or event"/></div><select value={source} onChange={e=>setSource(e.target.value)}><option value="all">All reliable sources</option><option value="regulator">Regulators</option><option value="exchange">Exchange</option><option value="company">Company sources</option><option value="news">News discovery</option></select><button className="primary-action" onClick={load}>Search</button></div><div className="evidence-list">{rows.map(r=><article className="evidence-card" key={r.id}><div className="evidence-source"><ShieldCheck size={17}/><span>{r.source_name}</span><small>{r.validation_status==="official"?"Official source":r.source_type}</small></div><div className="evidence-content"><div className="evidence-title-row"><h3>{r.title}</h3>{r.symbol&&<span className="symbol-chip">Matched to {r.symbol}</span>}</div>{r.summary&&<p>{r.summary}</p>}<div className="evidence-meta"><span>{r.published_at?new Date(r.published_at).toLocaleDateString():"—"}</span><span>{String(r.evidence_type||"market event").replace(/_/g," ")}</span><span>Relevance {Math.round(Number(r.relevance_score||0))}/100</span><span>Authority {Math.round(Number(r.authority_score||0))}/100</span><a href={r.source_url} target="_blank" rel="noreferrer">Open source <ExternalLink size={13}/></a></div>{r.matched_terms?.length?<div className="matched-terms">Matched: {r.matched_terms.slice(0,5).join(", ")}</div>:null}</div></article>)}{!rows.length&&<div className="empty-state"><h3>No verified evidence loaded</h3><p>Run “Sync Public Evidence,” then refresh. Undated and generic institutional pages are excluded.</p></div>}</div></section>}
