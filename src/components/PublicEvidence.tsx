import { useEffect,useState } from "react";
import { ExternalLink, RefreshCw, Search, ShieldCheck } from "lucide-react";
import { supabase } from "../lib/supabase";
type Evidence={id:string;source_name:string;source_type:string;title:string;published_at:string|null;symbol:string|null;summary:string|null;source_url:string;authority_score:number|null;fetched_at:string};
export default function PublicEvidence(){const[rows,setRows]=useState<Evidence[]>([]),[query,setQuery]=useState(""),[source,setSource]=useState("all"),[loading,setLoading]=useState(false);async function load(){setLoading(true);let q=supabase.from("internet_evidence").select("id,source_name,source_type,title,published_at,symbol,summary,source_url,authority_score,fetched_at").order("published_at",{ascending:false}).limit(300);if(query.trim())q=q.or(`title.ilike.%${query.trim()}%,summary.ilike.%${query.trim()}%,symbol.ilike.%${query.trim()}%`);if(source!=="all")q=q.eq("source_type",source);const{data}=await q;setRows((data||[]) as Evidence[]);setLoading(false)}useEffect(()=>{load()},[]);return <section className="system-page"><div className="system-heading"><div><div className="eyebrow">REAL-WORLD EVIDENCE</div><h1>Public Evidence Feed</h1><p>Official notices, regulatory updates, prospectuses, and selected market news used by the cross-reference engine.</p></div><button className="secondary-action" onClick={load}><RefreshCw size={16}/>{loading?"Refreshing…":"Refresh"}</button></div><div className="evidence-toolbar"><div className="search-field"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} placeholder="Search company, symbol, or event"/></div><select value={source} onChange={e=>setSource(e.target.value)}><option value="all">All sources</option><option value="regulator">Regulators</option><option value="exchange">Exchange</option><option value="news">News</option><option value="company">Company sources</option></select><button className="primary-action" onClick={load}>Search evidence</button></div><div className="evidence-list">{rows.map(r=><article className="evidence-card" key={r.id}><div className="evidence-source"><ShieldCheck size={17}/><span>{r.source_name}</span><small>{r.source_type}</small></div><div className="evidence-content"><div className="evidence-title-row"><h3>{r.title}</h3>{r.symbol&&<span className="symbol-chip">{r.symbol}</span>}</div>{r.summary&&<p>{r.summary}</p>}<div className="evidence-meta"><span>{r.published_at?new Date(r.published_at).toLocaleDateString():"Date unavailable"}</span><span>Authority {Math.round(Number(r.authority_score||0))}/100</span><a href={r.source_url} target="_blank" rel="noreferrer">Open source <ExternalLink size={13}/></a></div></div></article>)}{!rows.length&&<div className="empty-state"><h3>No public evidence loaded yet</h3><p>Run the “Sync Public Evidence” GitHub Action, then refresh this page.</p></div>}</div></section>}
