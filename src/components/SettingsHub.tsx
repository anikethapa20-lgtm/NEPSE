import { useState } from "react";import DataQuality from "./DataQuality";import AdminPanel from "./AdminPanel";import DecisionsPanel from "./DecisionsPanel";
type View="quality"|"admin"|"decisions";
export default function SettingsHub({projectId}:{projectId:string}){const[view,setView]=useState<View>("quality");return <section><div className="page-tabs"><button className={view==="quality"?"active":""} onClick={()=>setView("quality")}>Data quality</button><button className={view==="admin"?"active":""} onClick={()=>setView("admin")}>System status</button><button className={view==="decisions"?"active":""} onClick={()=>setView("decisions")}>Research decisions</button></div>{view==="quality"&&<DataQuality projectId={projectId}/>} {view==="admin"&&<AdminPanel projectId={projectId}/>} {view==="decisions"&&<DecisionsPanel projectId={projectId}/>}</section>}
