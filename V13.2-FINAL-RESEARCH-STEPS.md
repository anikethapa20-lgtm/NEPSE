# V13.2 Final Research Completion

1. Run `supabase/upgrade_v13_2_research_completion.sql` in Supabase SQL Editor after the earlier migrations.
2. Confirm GitHub secrets `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are present.
3. Run **Complete Research Pipeline** from GitHub Actions.
4. The validation step passes only when market data exists, every event is completed, every completed event has a terminal outcome and cross-reference timestamp, and no event is failed.
5. `unexplained`, `requires_review`, and `insufficient_data` are valid completed research outcomes. They do not prevent 100% processing.
6. In Supabase, run `select * from get_research_readiness('<PROJECT_UUID>');` to inspect publication readiness.

Expected historical market rows: 530,674. Treat any different count as a dataset-version difference that must be documented rather than silently overwritten.
